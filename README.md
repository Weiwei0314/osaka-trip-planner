# osaka-trip-planner（旅行行程表）

離線可用的旅行行程規劃 PWA（Progressive Web App）。沒有後端、沒有建置流程，資料全部存在瀏覽器本機（`localStorage`），可以安裝到手機主畫面當作一般 App 使用。

## 功能

- **多行程管理**：可同時建立多個行程，列表會顯示狀態（即將出發／旅行中／已結束）、倒數天數、行程項目數與未完成代辦數。
- **總覽**：進入行程後預設顯示的分頁。顯示出發倒數／旅行中狀態、行程項目數、代辦完成度、購物預估金額，若今天正好在旅行日期範圍內會列出今日行程，並列出未完成的代辦事項。
- **時間軸**：依行程天數分組顯示行程項目，可設定分類（景點／餐廳／交通／住宿／購物／其他）、時間（輪盤選擇器）、地點、備註、最多 20 張照片；支援拖曳調整順序／換天／與其他項目互換時段；可在每個行程項目下掛購物、代辦子項目。
- **購物清單**：品項、數量、金額、分類、所屬天數、備註、照片，自動加總已購買數量與預估金額。
- **代辦事項**：事項、截止日期、優先度（高／中／低）、所屬天數、備註、照片，依優先度排序。
- **複製到其他行程**：行程項目、購物、代辦事項都可以複製一份到另一個行程，複製後兩邊資料各自獨立、互不影響。
- **CSV 匯入**：進入行程詳細頁後，右上角的文件圖示可匯入 CSV 檔案，一次匯入多筆行程項目／購物／代辦／地點收藏（格式見下方）。
- **JSON 匯出／匯入**：在行程列表可以把單一行程（含所有子項目）匯出成 JSON 檔分享，或用左上角「匯入」按鈕匯入別人分享的行程檔案（會建立成一個全新行程）。
- **離線支援**：透過 Service Worker（`sw.js`）快取靜態資源，離線也能開啟使用。

## 如何執行

純靜態網頁，沒有 npm 套件、沒有建置步驟：

- 直接用瀏覽器開啟 `osaka/home.html`，或
- 用任意靜態伺服器在本機啟動（例如 `npx serve .`），透過 `http://localhost/osaka/` 開啟——若要測試「安裝到主畫面」或離線快取（Service Worker），需要走 `http://` 或 `https://`，不能是 `file://`。

## 檔案結構

所有網頁檔案都在 `osaka/` 資料夾底下：

| 檔案 | 說明 |
|---|---|
| `osaka/home.html` | 唯一的頁面，所有畫面（行程列表／時間軸／購物／代辦／總覽）都是同一個頁面裡切換 |
| `osaka/app.js` | 全部邏輯：資料層（`DB`）、渲染、CSV/JSON 匯入匯出、時間輪盤選擇器、拖曳排序 |
| `osaka/style.css` | 樣式 |
| `osaka/sw.js` | Service Worker，離線快取用；改動 `home.html`／`app.js`／`style.css` 後記得同步調高 `CACHE` 版本號，否則已安裝的使用者會讀到舊快取 |
| `osaka/manifest.json` | PWA 設定（圖示、名稱、standalone 模式） |
| `osaka/icon.svg` | 舊版 App 圖示（原始向量版），目前沒有任何地方引用 |
| `osaka/icon-152.png`／`icon-180.png` | iOS「加入主畫面」用的圖示（Apple 只認 PNG，不吃 SVG） |
| `osaka/icon-192.png`／`icon-512.png` | Android／PWA `manifest.json` 用的圖示 |

## 資料儲存與限制

行程／事件／購物／代辦這些文字資料存在瀏覽器的 `localStorage`（每個網站通常只有 5–10MB 硬性配額）。**照片則存在 `IndexedDB`**（`trip-planner-photos` 資料庫），以原始二進位 Blob 儲存、不經過 Base64 膨脹，配額比 `localStorage` 大得多（通常是裝置可用空間的一個比例），兩者互相獨立，照片不會再擠壓到文字資料的配額。目前每個項目最多 20 張照片（`MAX_PHOTOS_PER_ITEM`），上傳時會自動壓縮到最長邊 1600px、JPEG 品質 0.8。

實作重點（如果要繼續修改這部分程式碼）：
- 每個項目的 `photos` 欄位只存照片在 IndexedDB 裡的 id，不是圖片本身；讀取時要透過 `getPhotoUrl(id)`（非同步，內部用 `URL.createObjectURL` 快取）換成可以放進 `<img src>` 的網址。
- 因此所有會顯示照片的渲染函式（`photoThumbsHtml`、`eventItem`、`shopItem`、`todoItem`、`renderTimeline`、`renderShopping`、`renderTodo`、`renderOverview`、`renderDetail`、`render`、`renderPhotoStrip`）都是 `async`。
- 刪除項目／行程、在編輯時移除照片、或另存副本時，都要記得同步清掉 IndexedDB 裡對應的 Blob（見 `deletePhotoBlobs`／`duplicatePhotoBlobs`），避免留下孤兒資料或兩份資料共用同一張照片。
- JSON 匯出／匯入會在檔案邊界把照片轉回／轉出 Base64（`inlinePhotosForExport`／`importPhotosFromDataUrls`），確保匯出的 JSON 檔仍然是自包含、可攜的單一檔案。
- 第一次載入時會自動把舊版本（照片直接存 Base64 在 localStorage）的資料搬進 IndexedDB（`migratePhotosToIndexedDB`），只會執行一次。

## CSV 匯入格式

標頭列（順序不限）：

```
類型,標題,日期,開始時間,結束時間,分類,地點,數量,金額,截止日期,優先度,備註
```

| 欄位 | 說明 |
|---|---|
| 類型（必填） | `行程`/`event`、`購物`/`shopping`、`代辦`/`todo`、`地點`/`location` |
| 標題（必填） | 項目名稱 |
| 日期 | `YYYY-MM-DD` 或 `YYYY/MM/DD`；也接受 Excel 日期序列數字。行程/購物用作所屬日期，代辦用作排入哪一天 |
| 開始時間／結束時間 | 只有「行程」項目會用，`HH:MM` 格式（也接受帶 AM/PM 的格式） |
| 分類 | 行程/地點用：景點、餐廳、交通、住宿、購物、其他；購物則是自由文字 |
| 優先度 | 只有「代辦」會用：高/中/低 |
| 備註 | 各類型通用 |

不需要的欄位留空即可。用 Excel 存檔時建議選「**CSV UTF-8（逗號分隔）**」而不是一般的「CSV（逗號分隔）」，否則中文字會因編碼不同變成亂碼。

## 更新紀錄

> 以下時間是實際修改檔案的時間戳記核對後回填的。2026-07-13 10:04 與 11:23 這兩個時間點有直接查證磁碟時間戳記；13:40 與 13:57 是這次回填當下查到的最新時間。IndexedDB 遷移與「手機實測後三項修正」中間的確切分界，因為 `app.js` 被多次覆寫、沒有留下中間時間點可查證，只能給合理排序、不保證逐分鐘準確。2026-07-10 的五筆記錄則是直接讀取 git commit 時間戳記回填，分秒準確。

### 2026-07-13 14:17 — 換成自訂手繪風圖示
- 把使用者提供的手繪風插圖（手帳＋飛機＋地圖釘＋行李箱）處理成正式圖示：自動偵測並裁掉原圖四周的米白留白、置中裁切成正方形，輸出 152/180/192/512px 四種尺寸
- `home.html`、`manifest.json` 的圖示參照都改指向新圖；瀏覽器分頁 favicon 也一併換成新圖（原本是 `icon.svg`）
- 舊的 `icon.svg`（藍底飛機）目前保留在資料夾裡但沒有任何地方引用，可自行決定要不要刪除
- Service Worker 快取版本更新至 `trip-planner-v17`

### 2026-07-13 13:57 — 修正主畫面圖示
- 新增 `icon-152.png`／`icon-180.png`／`icon-192.png`／`icon-512.png`，修正 iOS「加入主畫面」只顯示行程表名稱第一個字（「行」）的問題——原因是 `apple-touch-icon` 原本只提供 SVG，但 iOS 該標籤不支援 SVG，讀不到就會自動退回用文字產生圖示
- `manifest.json` 的 `icons` 補上 PNG 版本，Android 的安裝提示也會用到
- Service Worker 快取版本更新至 `trip-planner-v16`

### 2026-07-13 13:40 — 手機實測後修正
- 關閉手機瀏覽器的雙指縮放（`viewport` 加上 `maximum-scale=1.0, user-scalable=no`），改善原生 App 的操作手感
- 「總覽」新增「整趟行程一覽」：列出每一天的日期與當天第一個行程項目（或「尚無安排」），點一列會跳到「時間軸」分頁並自動捲動到那一天；原本「今天」的詳細區塊維持不變
- 修正 JSON／CSV 匯入在照片較多時感覺像當機的問題：改成一次平行處理所有照片（原本是一張一張依序處理），並在匯入／匯出過程中顯示「處理中」的讀取畫面，不再是完全沒有回饋的空白等待
- Service Worker 快取版本更新至 `trip-planner-v15`

### 2026-07-13（約 11:23–13:40 之間，確切時間點無法查證）— IndexedDB 照片儲存遷移
- 照片改存 `IndexedDB`（原本是 Base64 文字塞進 `localStorage`），不再共用文字資料的 5–10MB 配額
- 每個項目照片上限從 6 張提高到 20 張，壓縮設定放寬為最長邊 1600px、JPEG 品質 0.8
- 舊版資料會在第一次載入時自動搬遷到 IndexedDB，不需要手動處理
- 複製到其他行程時，照片會複製一份全新的（不再共用同一份），避免刪其中一邊連帶影響另一邊
- JSON 匯出／匯入的檔案格式維持自包含（照片仍以 Base64 內嵌在檔案裡），只是行程內部的儲存方式改變
- Service Worker 快取版本更新至 `trip-planner-v14`

### 2026-07-13 11:23
- 新增「總覽」分頁，成為進入行程後的預設畫面
- 新增「複製到其他行程」功能（行程項目／購物／代辦皆可複製到別的行程，各自獨立）
- 修正：行程項目若日期不在目標行程的日期範圍內（例如複製自其他行程、或目標行程尚未設定日期），先前在時間軸完全不會顯示；現在會歸類到「整個行程」區塊顯示，購物／代辦的無日期項目也一併整理進同一個區塊
- 修正：沒有設定日期的行程，只要購物／代辦清單裡有資料，時間軸就會正常顯示，不再只死板地卡在「請先設定日期」的提示
- Service Worker 快取版本更新至 `trip-planner-v13`
- README.md 建立

### 2026-07-13 10:04
- 新增 CSV 匯入功能（可匯入行程項目／購物／代辦／地點收藏，支援 Excel 日期序列與多種日期格式）
- Service Worker 快取版本更新至 `trip-planner-v11`

### 2026-07-10 21:30 — 新增行程匯出／匯入分享功能
- 行程列表每張卡片加上「匯出」按鈕，把該行程（含所有行程項目、購物、代辦、地點、照片）打包成 JSON 檔，支援手機原生分享（AirDrop／LINE／Email）或退回一般下載
- 行程列表左上角按鈕改為「匯入」，讀取分享來的 JSON 檔並以全新 ID 加入自己的清單，避免與既有資料衝突
- Service Worker 快取版本更新至 `trip-planner-v10`

### 2026-07-10 21:17 — 行程／購物／代辦新增照片功能
- 三種項目的編輯視窗都加上「照片」欄位，可從相機拍照或相簿選圖（當時上限 6 張／項目，後於 IndexedDB 遷移時提高到 20 張）
- 選取後自動壓縮（最長邊 1000px、JPEG 品質 0.68）以縮小 `localStorage` 占用（後於 IndexedDB 遷移時放寬到 1600px／0.8）
- 卡片列表顯示縮圖，點擊可全螢幕放大檢視（lightbox）
- `localStorage` 寫入加上容量不足時的錯誤提示，避免靜默失敗
- Service Worker 快取版本更新至 `trip-planner-v9`

### 2026-07-10 20:39 — 修正手機版多項顯示與效能問題
- 修正 header／tab bar 固定高度未計入安全區導致內容被壓縮的 bug（+ 按鈕看不到、tab 圖示過小的根因）
- 放大底部 tab bar 圖示與各小按鈕的觸控熱區（維持視覺大小、擴大實際可點擊範圍）
- 拖曳幽靈卡片加上寬度／位置夾限，避免超出手機螢幕
- 全域加上 `touch-action: manipulation` 消除行動裝置點擊延遲
- 時間軸渲染時購物／代辦改成預先分組查詢，避免對每個行程項目重複掃描整個清單
- 加上 `overflow-x: hidden` 防止任何元素造成整頁橫向溢出
- Service Worker 快取版本更新至 `trip-planner-v8`

### 2026-07-10 20:12 — 時間輸入改滾輪選取、行程卡片可拖曳
- 開始時間改為滾輪選取（上午／下午、小時、分鐘），停留時間輸入後自動換算結束時間
- 行程項目新增拖曳把手：拖到同天另一張卡片會交換時間，拖到別天區塊會換天並保留時間
- 滾輪加入滑鼠拖曳捲動支援，改善桌機操作手感
- Service Worker 改為網路優先策略，避免卡在舊版快取
- Service Worker 快取版本更新至 `trip-planner-v7`

### 2026-07-10 19:23 — 代辦／購物與時間軸互通、修正日期少一天 bug
- 時間軸現在也會顯示直接在代辦／購物分頁新增（未掛在行程項目下）的項目
- 修正 `getDays()`／`todayStr()` 因 `toISOString` 時區轉換導致日期少一天的問題
- 行程項目新增「移動到別天」功能，保留原本時間／內容並檢查時間衝突
- Service Worker 快取版本更新至 `trip-planner-v4`
